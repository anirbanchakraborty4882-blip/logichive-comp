import { EmployeeProfile } from '../types';

// Production default: NO pre-created sample employees.
// Initial employee collection is empty until real users register.
export const INITIAL_EMPLOYEES: EmployeeProfile[] = [];
